# خريطة الكود – Code Map

> هذا الملف يُنشأ ويُحدَّث آليًا بواسطة الدور `role-code-mapper`.  
> في النسخة الحالية من القالب، قد يكون هذا الملف مجرد قالب فارغ حتى يتم تشغيل دور اكتشاف الكود على مشروع حقيقي يحتوي كودًا فعليًا.

## جذور الكود (Code Roots)

> سيتم ملء هذا القسم تلقائيًا بجذور الكود (Backends/Frontends/Services/Libraries) عندما يتم تشغيل `role-code-mapper` على الريبو الفعلي.

| RootName | نوع الجذر (Backend/Frontend/Service/Library) | المسار (Path) | اللغات/التقنيات الرئيسية | ملاحظات |
|----------|----------------------------------------------|---------------|---------------------------|---------|
| _(auto)_ | _(auto)_                                     | _(auto)_      | _(auto)_                  | _(auto)_ |

## نطاقات فحص الكود (Code Scan Scopes)

> يتم مزامنة هذا الجدول مع جدول Scopes الموجود في `.info/progress.md`، مع تفاصيل إضافية عند الحاجة.

| Scope Name | RootName | المسار (Path) | الحالة | ملاحظات |
|-----------|----------|----------------|--------|---------|
| _(auto)_  | _(auto)_ | _(auto)_       | _(auto)_ | _(auto)_ |

## ملاحظات

- عند تشغيل `role-code-mapper` على مشروع يحتوي كودًا فعليًا:
  - سيتم استبدال الصفوف المؤقتة أعلاه ببيانات حقيقية.
  - يمكن للأدوار الأخرى (`role-code-analyzer`, `role-spec-from-code`, `role-feature-mapper`, `role-spec-orchestrator`) الاعتماد على هذا الملف لفهم بنية الكود وربطه بالميزات وSpec Kit.


